<nav class="bg-indigo-600">
  <div class="container mx-auto px-4">
    <div class="flex justify-between items-center h-16">
      <div class="flex items-center">
        <%= link_to 'Auction System', root_path, class: 'text-white font-semibold text-lg' %>
      </div>

      <div class="flex items-center">
        <% if user_signed_in? %>
          <div class="flex items-center space-x-4">
            <span class="text-white"><%= current_user.name %></span>
            <%= link_to 'All Auctions', root_path, class: 'text-white hover:text-indigo-100' %>
            <%= link_to 'My Auctions', auctions_path(filter: 'mine'), class: 'text-white hover:text-indigo-100' %>
            <%= link_to 'My Bids', bids_path, class: 'text-white hover:text-indigo-100' %>
            <%= button_to 'Logout', destroy_user_session_path, method: :delete, class: 'text-white hover:text-indigo-100' %>
          </div>
        <% else %>
          <%= link_to 'Login', new_user_session_path, class: 'text-white hover:text-indigo-100' %>
        <% end %>
      </div>
    </div>
  </div>
</nav> 